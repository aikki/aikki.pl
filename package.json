{
  "devDependencies": {
    "sass": "^1.85.0"
  },
  "scripts": {
    "scan:icons": "make icons",
    "sass:watch": "sass assets/styles.scss assets/styles.css -w",
    "presass:build": "npm run scan:icons",
    "sass:build": "sass assets/styles.scss assets/styles.css"
  }
}
